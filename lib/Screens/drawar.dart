import 'package:flutter/material.dart';

class DrawarTab extends StatelessWidget {
  const DrawarTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(
          height: 100,
          decoration: BoxDecoration(color: Colors.green),
        )
      ],
    );
  }
}
